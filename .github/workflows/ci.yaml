name: CI

on:
  push:
    branches:
      - '!master'
  pull_request:
    branches:    
      - 'master'

jobs:
  setup:
    uses: ./.github/workflows/build.yaml
  check:
    uses: ./.github/workflows/build.yaml
  test:
    uses: ./.github/workflows/test.yaml
  build:
    uses: ./.github/workflows/check.yaml